#!/usr/bin/env bash

# brew install --cask nikitabobko/tap/aerospace -- https://nikitabobko.github.io/AeroSpace/guide
brew install --cask bitwarden
brew install --cask cursor
brew install --cask dbeaver-community
brew install --cask font-meslo-lg-nerd-font
brew install --cask ghostty
brew install --cask github
brew install --cask google-chat
brew install --cask google-chrome
brew install --cask google-cloud-sdk
brew install --cask google-drive
brew install --cask handbrake
brew install --cask keycastr
brew install --cask mpv
brew install --cask ngrok
brew install --cask obsidian
brew install --cask oracle-jdk
brew install --cask postman
brew install --cask slack
brew install --cask visual-studio-code
brew install --cask zoom

brew install docker
brew install docker-compose

brew install lua-language-server
brew install neovim
brew install starship

brew install apache-flink
brew install go
brew install lua
brew install node
brew install python
brew install rust
brew install terraform
brew install tmux
brew install uv

brew install ollama

brew install antigen && echo "🧬 Installed antigen — plugin manager for Zsh"
brew install bat && echo "🦇 Installed bat — cat clone with syntax highlighting and Git integration"
brew install cheat && echo "📘 Installed cheat — view and create interactive cheat sheets in the terminal"
brew install discord && echo "💬 Installed Discord — communication app for chat, voice, and video"
brew install fd && echo "🔎 Installed fd — simple, fast, user-friendly alternative to find"
brew install ffmpeg && echo "🎬 Installed ffmpeg — record, convert, and stream audio and video"
brew install fzf && echo "⚙️ Installed fzf — interactive fuzzy finder for command-line search"
brew install gh && echo "🐙 Installed GitHub CLI — interact with GitHub from the command line"
brew install git-delta && echo "🔧 Installed git-delta — syntax-highlighting pager for git and diff output"
brew install htop && echo "📊 Installed htop — interactive process viewer and system monitor"
brew install imagemagick && echo "🖼️ Installed ImageMagick — enables SVG, font, HEIC, and JPEG XL previews"
brew install jq && echo "📘 Installed jq — lightweight and flexible command-line JSON processor"
brew install jsonpp && echo "🪶 Installed jsonpp — pretty-prints JSON for easier reading"
brew install libomp && echo "⚙️ Installed libomp — OpenMP library required for parallel computation (used by XGBoost)"
brew install markdownlint-cli && echo "📝 Installed markdownlint-cli — lints Markdown files for style and consistency"
brew install poppler && echo "📄 Installed poppler — PDF rendering library (for previewing PDFs)"
brew install ripgrep && echo "🚀 Installed ripgrep — fast recursive search tool (grep alternative)"
brew install shellcheck && echo "🐚 Installed shellcheck — static analysis tool for shell scripts"
brew install stow && echo "📦 Installed GNU Stow — manages dotfiles and symlinked configurations"
brew install terraform-docs && echo "📘 Installed terraform-docs — generates documentation from Terraform modules"
brew install tflint && echo "🔍 Installed tflint — lints and validates Terraform code"
brew install tldr && echo "📚 Installed tldr — simplified, community-driven man pages"
brew install trash && echo "🗑️  Installed trash — safely moves files to the Trash instead of permanent deletion"
brew install tree && echo "🌳 Installed tree — displays directory structures in a tree-like format"
brew install wget && echo "🌐 Installed wget — downloads files from the web via HTTP, HTTPS, or FTP"
brew install yazi && echo "🦊 Installed yazi — fast terminal file manager written in Rust"
brew install yq && echo "🧩 Installed yq — command-line YAML processor (like jq but for YAML)"
brew install yt-dlp && echo "🎥 Installed yt-dlp — downloads videos from YouTube and other sites"
brew install zoxide && echo "⚡ Installed zoxide — smarter, faster cd command for quick directory jumping"
brew install zsh-syntax-highlighting && echo "✨ Installed zsh-syntax-highlighting — adds syntax coloring to Zsh commands"
brew install zsh-vi-mode && echo "🧠 Installed zsh-vi-mode — enables Vim-style keybindings in Zsh"

brew install helm
brew install kubernetes-cli
